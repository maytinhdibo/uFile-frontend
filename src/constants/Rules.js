export const emailRule = 'Must be email';

export const usernameRule = `A username must contain only letters and numbers character 
                            and should be between 6-20 characters`;

export const passwordRule = `A password must contain only letters and numbers characters 
                            and should be between 6-20 characters`;

export const confirmPasswordRule = 'Confirm password does not match';

export const fullnameRule = 'Your name must contain only letter and space and should be between 8-100 characters';
